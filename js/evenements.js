function tester()
{
    var nom=f.nom.value;
    var nombre=f.Nombre.value;
 if(nom.length==0){
	alert("taper le nom est prenom ");
	else 
	{
		if(nombre.length!=3)
		{
			alert("le champs de saisie doit contenir 3 caractere")
		}
	}
}